#!/usr/bin/env bash

set -e

BUILD_DIR=$1
CACHE_DIR=$2
ENV_DIR=$3

cd $BUILD_DIR/ember-orchard
yarn install
cd ../$SUBDIR
yarn install
ember deploy production
ember build --environment production
cp Procfile ..
exit 0
